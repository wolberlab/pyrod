get parameters for dist_dict, angl_dict from csd?

improve exclusion volume placement
  - create empty list with filtered exclusion volumes
  - with distance in 2 A steps starting at 2
    - find exclusion volumes within distance via tree query

logging